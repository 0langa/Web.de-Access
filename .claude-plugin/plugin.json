{
  "name": "webde-access",
  "version": "0.2.0",
  "description": "Private full-access WEB.DE mailbox connector for Claude Code, sharing the same MCP server as the Codex connector.",
  "author": {
    "name": "Local developer"
  },
  "skills": "./claude-code/skills/",
  "mcpServers": {
    "webde-access": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/mcp/server.mjs"
      ],
      "cwd": "${CLAUDE_PLUGIN_ROOT}"
    }
  }
}
